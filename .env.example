# Configuración de Base de Datos
DB_USER=postgres
DB_PASSWORD=tu_password_aqui
DB_HOST=localhost
DB_PORT=5432
DB_NAME=taveron

# Seguridad (JWT)
SECRET_KEY=genera_una_clave_segura_aqui
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Cifrado de Datos Sensibles (Fernet)
# Puedes generar una con: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=tu_llave_fernet_en_base64_aqui

# Entorno
ENVIRONMENT=development
